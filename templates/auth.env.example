# Placeholder only. Do not commit real credentials.
# Use --auth none for unauthenticated testing.
# Use --auth placeholder to create an authenticated testing scaffold.
# Accepted aliases include --auth no and --auth yes.
AUTH_ENABLED="false"
AUTH_MODE="none"
AUTH_METHOD=""
AUTH_USERNAME=""
AUTH_PASSWORD=""
AUTH_SESSION_FILE=""
AUTH_COOKIE_FILE=""
