target: "{{TARGET}}"
environment: "{{ENVIRONMENT}}"
login_path: "{{LOGIN_PATH}}"
allowed:
  - "{{TARGET}}"
excluded:
  - "destructive testing"
  - "denial of service"
  - "brute force"
  - "credential stuffing"
  - "race condition stress testing"
notes:
  - "Generated scope template. Review before enabling active checks."
